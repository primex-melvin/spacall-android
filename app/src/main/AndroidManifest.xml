<?xml version="1.0" encoding="utf-8"?>
<!-- app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ph.spacall.android">

    <!-- Internet permission for map tiles -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Network state for osmdroid -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Write external storage for osmdroid cache (optional) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spacall"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PhoneActivity"
            android:exported="false"/>

        <activity
            android:name=".OtpActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".UploadIdActivity"
            android:exported="false"/>

        <activity
            android:name=".FilterActivity"
            android:exported="false"/>

        <activity
            android:name=".PaymentActivity"
            android:exported="false"/>

        <activity
            android:name=".EnterAmountActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".ConfirmPaymentActivity"
            android:exported="false"/>

        <activity
            android:name=".VipSuccessActivity"
            android:exported="false"/>

        <activity
            android:name=".EnableLocationActivity"
            android:exported="false"/>

        <activity
            android:name=".SearchingTherapistsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"/>

        <activity
            android:name=".TherapistDetailActivity"
            android:exported="false"
            android:parentActivityName=".SearchingTherapistsActivity" />

        <activity
            android:name=".BookingRequestAcceptedActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".TherapistDetailActivity" />

        <activity
            android:name=".TherapistArrivedActivity"
            android:exported="false"
            android:parentActivityName=".BookingRequestAcceptedActivity" />

        <activity
            android:name=".SessionCountdownActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".TherapistArrivedActivity" />

        <activity
            android:name=".SessionFinishedActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".SessionCountdownActivity" />

    </application>

</manifest>
